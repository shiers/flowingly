# Set this to your Azure Container Apps backend URL when deploying to Azure Static Web Apps.
# Leave unset for local development (Vite proxy) and Docker Compose (nginx proxy).
# VITE_API_URL=https://api.<your-app>.azurecontainerapps.io
